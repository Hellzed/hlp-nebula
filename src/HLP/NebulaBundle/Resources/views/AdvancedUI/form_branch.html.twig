{% form_theme form 'HLPNebulaBundle:AdvancedUI:form_theme.html.twig' %}

<form role="form" class="mainForm row" action="{% if app.request.attributes.get('_route') == 'hlp_nebula_edit_branch' %}{{ path('hlp_nebula_edit_branch', {'owner': owner.nameCanonical, 'mod': mod.modId, 'branch': branch.branchId}) }}{% else %}{{ path('hlp_nebula_new_branch', {'owner': owner.nameCanonical, 'mod': mod.modId}) }}{% endif %}" method="post" {{ form_enctype(form) }}>
  <h4><span class="glyphicon glyphicon-asterisk"></span> Branch details</h4>
  <hr/>
  {{ form_widget(form) }}
  <button type="submit" class="btn btn-default">Submit</button>
</form>

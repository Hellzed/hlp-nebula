{% extends "HLPNebulaBundle::layout.html.twig" %}

{% block title %}
  {{ owner.name }} / {{ mod.title }} / Delete mod - {{ parent() }}
{% endblock %}

{% block body %}
<div class="row">
  <ol class="breadcrumb">
    <li><a href="{{ path('hlp_nebula_homepage') }}">Nebula</a></li>
    <li><a href="{{ path('hlp_nebula_owner', {'owner': owner.nameCanonical}) }}">{{ owner.name }}</a></li>
    <li href="active">Delete mod : <a href="{{ path('hlp_nebula_branch', {'owner': owner.nameCanonical, 'mod': mod.modId}) }}">{{ mod.title }}</a></li>
  </ol>
</div>
<div class="row">
  <div class="col-sm-3">
    <h2><small>Delete mod </small>{{ mod.title }}</h2>
    <hr/>
    <p class="lead"><small class="text-muted">as </small>{{ owner.name }}</p>
    <hr/>
    <p class="lead"><small class="text-muted">or </small><a href="{{ path('hlp_nebula_owner', {'owner': owner.nameCanonical}) }}" class="btn btn-default">Cancel</a></p>
  </div>
  <div class="col-sm-9">
    <form action="{{ path('hlp_nebula_delete_mod', {'owner': owner.nameCanonical, 'mod': mod.modId}) }}" method="post" class="mainForm row">
      <h4><span class="glyphicon glyphicon-warning-sign"></span> Do you really want to delete this mod ?</h4>
      <hr/>
      <p class="well">
        This action will delete mod <strong>"{{ mod.title }}" (id: {{ mod.modId }})</strong>, all the branches it contains, and all the builds they contain.
      </p>
      <hr/>
      <input type="submit" value="Proceed" class="btn btn-danger" />
      {{ form_rest(form) }}
    </form>
  </div>
</div>
{% endblock %}

{% form_theme form 'HLPNebulaBundle:AdvancedUI:form_theme.html.twig' %}

<form role="form" class="mainForm row" action="{{ path('hlp_nebula_new_branch', {'owner': owner.nameCanonical, 'mod': mod.modId}) }}" method="post" {{ form_enctype(form) }}>
  <h4><span class="glyphicon glyphicon-asterisk"></span> Branch details</h4>
  <hr/>
  {{ form_widget(form) }}
  <button type="submit" class="btn btn-default">Submit</button>
</form>
